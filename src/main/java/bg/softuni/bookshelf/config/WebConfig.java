package bg.softuni.bookshelf.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * Centralized configuration for Spring Web MVC.
 * This class can be used to define global rules for web-related concerns
 * such as formatters, converters, or interceptors.
 */
@Configuration
public class WebConfig implements WebMvcConfigurer {
    // This class is currently a placeholder
}
